import React from 'react';
import { render } from '@testing-library/react';
import Counter from './Counter';

describe('1 - Counter', () => {
  it('should increment the number when clicked', () => {
    const { findAllByTitle } = render(<Counter />);
    // const button =
  });
});
